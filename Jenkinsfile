pipeline {
     agent any
     stage {
         stage ("Build") {
           steps {
               sh 'echo "Hello World"'
               sh '''
                   ls -lah
               '''
           }
         }
     }
}
